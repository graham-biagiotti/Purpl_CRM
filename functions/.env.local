# Local overrides for Firebase Functions emulator.
# This file is loaded automatically by the emulator (not deployed to production).
# RESEND_API_KEY is set to a fake value so email functions run through the
# emulator without ever calling the real Resend API.
RESEND_API_KEY=test-key
