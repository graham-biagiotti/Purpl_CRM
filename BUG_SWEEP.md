# Purpl CRM — Bug Sweep Report

Generated by adversarial code review. Findings ranked Critical / High / Medium / Low.
Each includes file + function, repro scenario, root cause, proposed fix, and confirmation status.

---

## SUBSYSTEM 1: DB LAYER (db.js)

### [HIGH] Data Loss on Failed Debounced Write
**File:** db.js:284, 308-348
**Repro:**
1. User edits a record (cache mutated at line 435)
2. Within 500ms debounce window, network drops or permission denied
3. `_saveCollection` fails at line 336
4. `_saveRetries` logic retries (line 341), but ONLY if `_doSave` is called again
5. If no further edits trigger `_doSave`, the failed write is abandoned

**Root cause:** The debounce system doesn't distinguish between "first attempt" and "retry attempt". Failed batch writes are abandoned if the user doesn't trigger another save.
**Fix:** Mark failed keys in a separate retry queue and periodically retry them (e.g., every 30s) until success.
**Status:** confirmed

---

### [HIGH] Cache-Firestore Divergence on Immediate Write Failure
**File:** db.js:415-424, 436, 446, 451
**Repro:**
1. User clicks "Add Account": `DB.push('ac', newAccount)` at line 435
2. Cache updated immediately: `a.push(v)`
3. `_writeDoc(k, v)` called at line 436 (fire-and-forget)
4. Network fails: `.catch(e)` at line 418 logs warning but doesn't revert cache
5. User sees the item in UI, but it never persists to Firestore
6. Next session: item gone (lost)

**Root cause:** `_writeDoc` and `_deleteDoc` are asynchronous without cache rollback on failure. Cache is mutated before network roundtrip confirms.
**Fix:** Maintain a write queue and only confirm in cache after Firestore write succeeds, or add cache rollback on failure.
**Status:** confirmed — all three mutation methods (push, update, remove) have this flaw

---

### [HIGH] Snapshot Listener Race Condition During Render
**File:** db.js:167-186, 181
**Repro:**
1. Component renders iterating `DB.a('ac')` (e.g., `accounts.map(acc => ...)`)
2. During iteration, remote user edits an account
3. Snapshot fires at line 181: `this._cache[key] = snap.docs.map(...)` — array reference replaced
4. Iteration on the old reference continues safely (JS doesn't crash), but the UI won't reflect the update until next render

**Root cause:** Snapshot handler directly replaces `this._cache[key]` without coordinating with active renders. In practice, JS's single-threaded nature and `DB.a()` returning a copy (`[...this._cache[k]]`) mitigates crash risk, but stale reads are possible.
**Fix:** DB.a() already returns a shallow copy, reducing crash risk. For full safety, debounce snapshot processing or use a revision counter.
**Status:** confirmed — mitigated by DB.a() copy semantics but stale reads still possible

---

### [HIGH] No Atomicity for Multi-Collection Updates
**File:** db.js:454-481 (atomicUpdate)
**Repro:**
1. `DB.atomicUpdate()` modifies both 'ac' and 'iv' collections
2. Collection 'ac' batch write succeeds
3. Collection 'iv' batch write fails (permission denied or network)
4. Firestore has 'ac' updated but 'iv' not — inconsistent state

**Root cause:** Each collection is saved independently (line 478). No cross-collection Firestore transaction. The 50ms setTimeout doesn't guarantee all writes succeed together.
**Fix:** Use Firestore transactions for cross-collection writes, or implement a write-ahead log with reconciliation.
**Status:** confirmed — atomicUpdate is atomic only in the cache, not in Firestore

---

### [HIGH] Last-Write-Wins Conflict Resolution
**File:** db.js:438-448, 321
**Repro:**
1. User A opens account with `revenue: 100`
2. User B edits same account, sets `revenue: 200`, saves
3. User A (still on old snapshot) edits same account, sets `name: "NewName"`, saves
4. User A's `.set(item, { merge: true })` at line 321 overwrites User B's `revenue: 200`

**Root cause:** No conflict detection. The local item is a stale copy; remote changes to the same doc are overwritten without version checking.
**Fix:** Use read-before-write with version checking, or Firestore transactions for contested writes.
**Status:** confirmed — inherent to the architecture, not a simple fix

---

### [MEDIUM] Snapshot Updates Silently Dropped During Debounce
**File:** db.js:176-179
**Repro:**
1. User edits account A (cache marked dirty, 500ms debounce starts)
2. Remote user edits account B in the SAME collection (snapshot fires)
3. `_dirty` or `_saveDirtyKeys.has(key)` is true at line 176
4. Entire snapshot update for that collection is deferred
5. User must click "Load Changes" banner to see account B update

**Root cause:** Dirty flag blocks ALL snapshot updates for the collection, not just the specific doc being edited.
**Fix:** Track dirty state per-document instead of per-collection, so unrelated doc updates can apply.
**Status:** confirmed — data isn't lost (Firestore has it), but UI is stale

---

### [MEDIUM] atomicUpdate 50ms Race Window
**File:** db.js:470
**Repro:**
1. `DB.atomicUpdate()` modifies cache
2. During 50ms setTimeout before flush, remote snapshot fires
3. Snapshot replaces cache at line 181, overwriting atomicUpdate changes
4. The 50ms flush then writes the snapshot data (not the user's changes)

**Root cause:** No snapshot deferral during the atomicUpdate flush window.
**Fix:** Set a flag during atomicUpdate that defers snapshot processing until flush completes.
**Status:** confirmed — narrow window but possible under load

---

### [MEDIUM] beforeunload Handler Exists but May Not Fire
**File:** auth.js:156-158
**Repro:**
1. User edits a record, debounce timer starts (500ms)
2. User closes browser tab at 200ms
3. `beforeunload` fires and calls `DB._flushPendingSave()`
4. But `_flushPendingSave` must complete synchronously — Firestore writes are async
5. Browser may kill the page before writes complete

**Root cause:** `beforeunload` exists (auth.js:156) and calls `_flushPendingSave`, but async Firestore writes may not complete before page unload.
**Fix:** Use `navigator.sendBeacon()` for critical pending writes, or reduce debounce to 100ms.
**Status:** confirmed — beforeunload handler exists but effectiveness is limited

---

### [LOW] Config Doc Listener Silently Clears Cache on Missing Keys
**File:** db.js:204-208
**Repro:**
1. Config doc updated but missing a key (e.g., 'lf_skus' removed)
2. Snapshot fires, `data['lf_skus']` is undefined
3. Cache set to `[]` — silently clears the LF SKU list
4. UI shows empty product list until page refresh

**Root cause:** No null-check; missing keys default to empty array instead of preserving existing cache.
**Fix:** Only update cache if the key exists in the snapshot data: `if (data.hasOwnProperty(k)) ...`
**Status:** confirmed
